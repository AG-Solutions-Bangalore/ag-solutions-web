// import { lazy } from "react";
// import { Route } from "react-router-dom";
// import { v3Loaders } from "./lazyRoutes";

/**
 * Future Version 3 (v3) Route Definitions
 * When adding V3 pages:
 * 1. Create components in @/features/v3/<feature>/pages/<Page>.tsx
 * 2. Add loader in lazyRoutes.ts under v3Loaders
 * 3. Add <Route> below and import in App.tsx / routes index!
 */

export const renderV3Routes = () => (
  <>
    {/* e.g. <Route path="home-v3" element={<HomePageV3 />} /> */}
  </>
);
